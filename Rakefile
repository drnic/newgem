require 'rubygems'
require 'rake'
require 'rake/clean'
require 'fileutils'
include FileUtils

$:.unshift 'lib'
require 'config/hoe' # setup Hoe + all gem configuration
require 'newgem/tasks' # load /tasks/*.rake

